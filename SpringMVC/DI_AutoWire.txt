@AutoWire

DI에 등록만 해놓으면 ---> 자동으로 역주입이 가능(해당 객체를 받는 생성자를 작성)
DI등록을 할때는 패키지 위치는 상관없음
DI에 등록이 안된 클래스(Service는 최초 생성된 패키지 내에 위치해야됨)

Bean으로 만들어진 객체들을 자동으로 연결

DAO, Controller, Service
스프링 프로젝트에서 구조로 사용하게될 클래스는 반드시
처음 생성된 패키지 내에서만 만들어야됨